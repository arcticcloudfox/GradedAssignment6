import "./styling.css"

export default function Banner(){ //The <p> tag is just a placeholder message
    return (
        <div>
        <header>Orbit Report</header>
        <p>Click on the buttons to see the satellites in that orbit type</p>
        </div>
    )
}

